spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:;DB_CLOSE_ON_EXIT=TRUE;MODE=MySQL
    username: test
    password:
moira:
  web:
    allow-origin:
      - http://localhost:8080
      - http://localhost:3000
      - http://192.168.1.6:5875
server:
  tomcat:
    basedir: logs/tomcat
    accesslog:
      enabled: true

logging.level.Exposed: DEBUG
