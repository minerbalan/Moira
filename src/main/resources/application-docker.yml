spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://moira-db/moira?reWriteBatchedInserts=true&serverTimezone=JST
    username: dbuser
    password: password
moira:
  web:
    allow-origin:
      - http://localhost:18080
